import 'package:flutter/cupertino.dart';

class OneChat extends StatelessWidget {
  const OneChat({data, super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
